{
  "name": "@natsatopics/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types, content-guide loading, Gemini client, pillar tracking and social-platform formatting used by both the functions backend and the web editor.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p . && node scripts/copy-guide.js",
    "lint": "tsc -p . --noEmit"
  },
  "dependencies": {
    "google-auth-library": "^9.14.1",
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.9"
  }
}
