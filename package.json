{
  "name": "natsatopics",
  "private": true,
  "version": "1.0.0",
  "description": "Natsastore daily social media content pipeline: YLE scraping, AI drafting, editor review via magic link, multi-platform publishing.",
  "workspaces": [
    "packages/shared",
    "functions"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "build:functions": "npm run build -w functions",
    "build:web": "npm --prefix web run build",
    "build": "npm run build:shared && npm run build:functions && npm run build:web",
    "lint": "npm run lint -w packages/shared && npm run lint -w functions && npm --prefix web run lint",
    "dev:web": "npm --prefix web run dev",
    "functions:shell": "npm run shell -w functions",
    "emulators": "firebase emulators:start --only functions,firestore,hosting"
  },
  "engines": {
    "node": ">=20"
  }
}
