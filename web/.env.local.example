# Copy to .env.local for local development.
# Value comes from `firebase deploy --only functions` output, or from
# running the functions emulator (http://127.0.0.1:5001/natsatopics/europe-west1/editorApi).
NEXT_PUBLIC_EDITOR_API_URL=http://127.0.0.1:5001/natsatopics/europe-west1/editorApi
